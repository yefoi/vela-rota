# Lectura por IA (opcional). Sin OPENAI_API_KEY oficia el oráculo local.
# OpenCode Zen (la key empieza con sk-): base https://opencode.ai/zen/v1
OPENAI_API_KEY=
OPENAI_BASE_URL=https://opencode.ai/zen/v1
OPENAI_MODEL=gpt-5.6-luna
OPENAI_FALLBACK_MODEL=deepseek-v4-flash

# Forzar el protocolo si la autodetección falla: chat | responses
# OPENAI_API

# Puerto del oficio (solo dev local)
PORT=8787
